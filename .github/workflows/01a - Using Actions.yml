name: "01a - Using Actions"

on:
  push:
    branches:
      - idonotexist

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      # https://github.com/marketplace/actions/checkout
      - name: Checkout repository # Why is it called "checkout", and not "clone"?!
        uses: actions/checkout@2.3.3

      - name: List contents
        run: Get-ChildItem
        shell: pwsh